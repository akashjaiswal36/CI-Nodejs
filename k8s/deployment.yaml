apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-akk
spec:
  replicas: 2
  selector:
    matchLabels:
      app: devops-akk
  template:
    metadata:
      labels:
        app: devops-akk
    spec:
      containers:
      - name: devops-akki
        image: devops-akki:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3001
        env:
        - name: APP_VERSION
          value: "v1"
        - name: ENV
          value: "local"
        readinessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 10
          periodSeconds: 20
